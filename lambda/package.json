{
  "name": "trc-yoga-ical",
  "version": "1.0.0",
  "description": "Lambda to aggregate yoga events from iCal feeds",
  "scripts": {
    "build": "tsc",
    "package": "rm -rf dist && npm run build && mkdir -p dist/node_modules && cp -r node_modules/eta dist/node_modules/eta && cd dist && zip -r ../lambda.zip .",
    "test": "jest"
  },
  "dependencies": {
    "@aws-sdk/client-bedrock-runtime": "^3.600.0",
    "@aws-sdk/client-dynamodb": "^3.600.0",
    "@aws-sdk/client-eventbridge": "^3.600.0",
    "@aws-sdk/client-s3": "^3.600.0",
    "@aws-sdk/client-sqs": "^3.600.0",
    "eta": "^3.5.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.140",
    "@types/jest": "^30.0.0",
    "@types/node": "^20.14.0",
    "jest": "^30.2.0",
    "ts-jest": "^29.4.6",
    "typescript": "^5.5.0"
  }
}
